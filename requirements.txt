# Core dependencies
Django>=5.0,<5.1

# Data visualization
plotly
d3
numpy
pandas

# Requests for API calls
requests

# Environment variables (optional)
python-dotenv

# Database (If using SQLite, remove `psycopg2-binary`)
# PostgreSQL support (remove if using SQLite)
psycopg2-binary

# Additional Django utilities
django-environ
